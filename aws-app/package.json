{
  "name": "hotel-occupancy-forecast-cdk",
  "version": "0.1.0",
  "private": true,
  "description": "Generated AWS-native CDK app for the hotel occupancy-forecast event model (event sourcing + SageMaker inference). Emitted by mermaid-event-model; verify-only, not for deployment as-is.",
  "bin": {
    "app": "bin/app.ts"
  },
  "scripts": {
    "emit": "node emit.mjs",
    "build": "tsc",
    "synth": "cdk synth",
    "diff": "cdk diff"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.14.0",
    "aws-cdk": "^2.150.0",
    "esbuild": "^0.21.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/client-kinesis": "^3.600.0",
    "@aws-sdk/client-sagemaker-runtime": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "aws-cdk-lib": "^2.150.0",
    "constructs": "^10.3.0",
    "ioredis": "^5.4.1",
    "ulid": "^2.3.0"
  }
}
